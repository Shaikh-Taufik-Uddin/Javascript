a = 5;
b = "5";
console.log(a == b); // == compares only the values not the types
console.log(a === b); // compares values along with types
// in cpp the stoi() function can parse values from string
